# ══════════════════════════════════════════════════════════════════
#  .env.template — copy this file to .env and fill in your values
#  $ cp .env.template .env
# ══════════════════════════════════════════════════════════════════

# ── Django ────────────────────────────────────────────────────────
# Keep this key as-is — it is tied to the existing database.
SECRET_KEY=ydjango-insecure-)1pp4qn7_p*e4nu6^(zzc^79$eg*ddg%94ib8v1_9$+cvz9x=4
DEBUG=True

# ── OpenAI ────────────────────────────────────────────────────────
# Obtain your key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# ── Neo4j ─────────────────────────────────────────────────────────
# Default values assume a local Neo4j instance on the standard port.
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=Turlangton35
NEO4J_DB=pubmedqa-context
